# Scripts

Place official RaspberryPi-ARM64 installation and deployment scripts in this folder.

Scripts should not hard-code `airpulse-user` or `/home/airpulse-user`. Resolve the target user and installation path at runtime and document all required privileges.
