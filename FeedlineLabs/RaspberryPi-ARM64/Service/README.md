# Service Definitions

Place official RaspberryPi-ARM64 service templates and related systemd files in this folder.

Service definitions should be generated or templated with the resolved AirPulse user, group, home directory, and installation path rather than containing hard-coded account names.
